# Local scratch/test area (owner's private testing)
IGNORETHISFOLDER/

# Playwright CLI session artifacts
.playwright-cli/

# Local run/commit helpers (owner-only, not part of the project)
commit.bat
serve.bat
